import React from 'react'

function Initialoffer({handleclick}) {
  return (
    <div>
      <button onClick={handleclick}>click me to see offer</button>
    </div>
  )
}

export default Initialoffer
